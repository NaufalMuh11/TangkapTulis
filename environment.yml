name: tf-handwriting
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - opencv-python-headless==4.8.1.78
      - numpy
      - pandas
      - matplotlib
      - tensorflow[and-cuda]
      - keras
      